const queryWrapper = document.querySelector('.query-wrapper');
const queryList = document.querySelector('.query-list');
const listWrapper = document.querySelector('.list-wrapper');
const validationMessage = document.querySelector('.notification');
const alertWrapper = document.querySelector('.alert-wrapper');
const loader = document.querySelector('.text-loader');
const loadMore = document.querySelector('.button-load');
const text = document.querySelector('.alert-text');

export {
  queryWrapper,
  queryList,
  listWrapper,
  validationMessage,
  alertWrapper,
  loader,
  loadMore,
  text,
};
